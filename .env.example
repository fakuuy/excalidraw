# Excalidraw MongoDB Backend - Environment Variables Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# API Base URL - Excalidraw API endpoint
VITE_API_BASE_URL=https://excalidraw.faku.pro/api

# Disable Firebase (leave empty)
VITE_APP_FIREBASE_CONFIG=

# Disable Excalidraw+ features (leave empty)
VITE_APP_PLUS_APP=false
VITE_APP_PLUS_LP=false

# Enable all collaboration features
VITE_APP_ENABLE_COLLABORATION=true
VITE_APP_ENABLE_WORKSPACES=true

# Custom branding
VITE_APP_NAME="Excalidraw - ITICA"
VITE_APP_DOMAIN="excalidraw.faku.pro"

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================

# MongoDB Connection - Uses existing itica_mongodb container
# Production: connects to your existing MongoDB
MONGODB_URI=mongodb://admin:your-mongo-password@itica_mongodb:27017/excalidraw?authSource=admin

# JWT Secret Key - MUST be long and secure (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-long-jwt-secret-key-here-minimum-32-chars

# CORS Configuration
FRONTEND_URL=https://excalidraw.faku.pro

# Server Port
PORT=3001

# Node Environment
NODE_ENV=production

# =============================================================================
# DOCKER/PRODUCTION SECRETS
# =============================================================================

# MongoDB Root Password - used in docker-compose
MONGO_ROOT_PASSWORD=your-secure-mongodb-root-password

# Redis Password (optional, for session caching)
REDIS_PASSWORD=your-redis-password-if-using-redis

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. NEVER commit this file with real values to git
# 2. Use strong passwords (20+ characters)
# 3. JWT_SECRET should be unique and very long
# 4. In production, consider using environment-specific secrets
# 5. Rotate these secrets periodically (every 6-12 months)

# =============================================================================
# EXAMPLE SECURE VALUES
# =============================================================================

# MONGO_ROOT_PASSWORD=Excalidraw2024!SecureMongoPass#$%
# JWT_SECRET=ExCaLiDrAw-JWT-2024-ITICA-Super-Long-Secret-Key-With-Numbers-123456789
# REDIS_PASSWORD=ReDiS-ExCaLiDrAw-Cache-2024-SecurePass!@#